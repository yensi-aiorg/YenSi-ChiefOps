# .env.example -- ChiefOps Local Dev Configuration
#
# Backend runs on HOST (not Docker) so all URLs use localhost.
# Copy to .env:  cp .env.example .env

# ── AI Configuration ──────────────────────────────────
AI_ADAPTER=cli                          # cli | openrouter | mock
AI_CLI_TOOL=claude                      # claude | codex | gemini
AI_CLI_TIMEOUT=120                      # Subprocess timeout in seconds
OPENROUTER_API_KEY=                     # Required only when AI_ADAPTER=openrouter
OPENROUTER_MODEL=anthropic/claude-sonnet-4-20250514

# ── Database ──────────────────────────────────────────
MONGO_URL=mongodb://localhost:23102
MONGO_DB_NAME=chiefops
REDIS_URL=redis://localhost:23103/0

# ── Citex RAG System (external Docker Compose) ───────
CITEX_API_URL=http://localhost:20161

# ── Privacy ───────────────────────────────────────────
PII_REDACTION_ENABLED=true

# ── Application ───────────────────────────────────────
LOG_LEVEL=INFO
UPLOAD_MAX_FILE_SIZE_MB=500
UPLOAD_MAX_BATCH_SIZE_MB=2048
ENVIRONMENT=development

# ── Frontend ──────────────────────────────────────────
VITE_API_URL=http://localhost:23101
VITE_WS_URL=ws://localhost:23101

# ── Port Allocation ──────────────────────────────────
# ChiefOps (this project):
#   Frontend:        23100  (Docker — Vite dev server)
#   Backend:         23101  (Host — uvicorn)
#   MongoDB:         23102  (Docker)
#   Redis:           23103  (Docker)
#
# Citex (separate project, ports 2016x):
#   Citex API:       20161
